use crate::traversal::pathfinding::{CandidatePath, PathFinder};

impl PathFinder {
    /// includes finding number of parts to split payment
    pub(super) fn find_path_mpp_payment(&mut self) -> Option<CandidatePath> {
        None
    }
}
